Thinks to do eventually
0 - Make Action IO Thread and return RUNNING/SUCCESS/FAILURE
1 - Working on a DSL
2 - Root.tick() completely deterministic returning Tick<Status, Context);
3 - Make time travel with Tick record;

API


    sequence("find home",
        belief("not at home now", (board) -> board.home == false),
        action("go forward", out(2, seconds()),(board) -> {
            while (board.home == false) {
                board.x += 10;
                board.home = board.x == 40;
            }
        }
        )
    ).with(board);

        [->] find home
                - (not at home now)
                - [go forward] for 2 seconds
