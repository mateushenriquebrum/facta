Thinks to do eventually
1 - Root.tick() completely deterministic returning Tick<Status, Context);
2 - Make time travel with Tick record;

API


    sequence("find home",
        belief("not at home now", (board) -> board.home == false),
        action("go forward", out(2, seconds()),(board) -> {
            while (board.home == false) {
                board.x += 10;
                board.home = board.x == 40;
            }
        }
        )
    ).with(board);

        [->] find home
                - (not at home now)
                - [go forward] for 2 seconds
